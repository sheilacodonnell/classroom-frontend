export function fetchAssignments(action) {
  // this is the action creator which creates an action object
  // what we dispatch is the action object
  // dispatches to the reducer

  return action

  // fetch('http://localhost:3000/api/teachers/1/assignments')
  // .then(res => res.json())
  // .then(data => console.log(data)) 
}